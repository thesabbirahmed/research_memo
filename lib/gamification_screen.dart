import 'package:flutter/material.dart';

class GamificationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gamification'),
      ),
      body: Center(
        child: Text('This is the Gamification page.'),
      ),
    );
  }
}
